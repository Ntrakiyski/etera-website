services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: etera_website
      POSTGRES_PASSWORD: payload
      POSTGRES_USER: payload
    ports:
      - "5432:5432"
    volumes:
      - etera_payload_postgres:/var/lib/postgresql/data

volumes:
  etera_payload_postgres:
